# 🏋️ AI Form Trainer — เอกสารพรีเซนต์โปรเจกจบ

> เว็บแอปพลิเคชันฝึกออกกำลังกายด้วย AI ที่วิเคราะห์ความถูกต้องของท่าทาง (form) แบบ real-time ผ่านกล้องเว็บแคม รองรับ 3 ท่า: **Squat (สควอท), Push-up (วิดพื้น), Plank (แพลงก์)** พร้อมระบบนับครั้ง/จับเวลา, เสียงพูดเตือนภาษาไทย, ระบบสมาชิก และแดชบอร์ดวิเคราะห์สถิติ

---

## 📑 สารบัญ
1. [ภาพรวมโปรเจกต์ (Elevator Pitch)](#1-ภาพรวมโปรเจกต์)
2. [ปัญหาและแรงจูงใจ](#2-ปัญหาและแรงจูงใจ)
3. [Tech Stack](#3-tech-stack)
4. [สถาปัตยกรรมระบบ (Architecture)](#4-สถาปัตยกรรมระบบ)
5. [Flow การทำงาน (Data Flow)](#5-flow-การทำงานตั้งแต่กล้องถึงผลลัพธ์)
6. [หัวใจของระบบ AI](#6-หัวใจของระบบ-ai-มีดีอยู่ตรงนี้)
7. [โครงสร้างโค้ด (Code Structure)](#7-โครงสร้างโค้ดแบบไฟล์ต่อไฟล์)
8. [ระบบสมาชิกและความปลอดภัย](#8-ระบบสมาชิก-auth)
9. [ฐานข้อมูล](#9-ฐานข้อมูล-database)
10. [จุดเด่นทางเทคนิคที่ควรเน้นตอนพูด](#10-จุดเด่นทางเทคนิคที่ควรเน้นตอนพูด)
11. [ข้อจำกัดและแผนพัฒนาต่อ](#11-ข้อจำกัดและแผนพัฒนาต่อ)
12. [บทพูดพรีเซนต์ (Script)](#12-บทพูดพรีเซนต์-พร้อมจับเวลา)
13. [คำถาม–คำตอบที่อาจโดนถาม (Q&A)](#13-คำถามคำตอบที่อาจโดนถาม)

---

## 1. ภาพรวมโปรเจกต์

**ชื่อระบบ:** AI Form Trainer (โค้ดเนมในอีเมลระบบคือ "Decepticon")

**คำอธิบายสั้น 1 ประโยค:**
> "ระบบเทรนเนอร์ส่วนตัวด้วย AI ที่ใช้กล้องคอมตรวจจับโครงกระดูกของผู้เล่นแบบ real-time แล้วใช้ Machine Learning จำแนกว่าฟอร์มท่าออกกำลังกายถูกหรือผิดตรงไหน พร้อมนับจำนวนครั้ง จับเวลา และพูดเตือนเป็นภาษาไทยทันที"

**ทำอะไรได้บ้าง:**
- เลือกท่าออกกำลังกายได้ 3 ท่า (Squat / Push-up / Plank)
- เปิดกล้อง → AI วาดโครงกระดูก (skeleton) ทับตัวผู้เล่น
- จำแนกฟอร์ม: ถูก (good) หรือ ผิดแบบไหน (เช่น ส้นเท้าลอย, หลังงอ, สะโพกตก, คอเงย)
- **Squat/Push-up:** นับจำนวนครั้ง แยก good/bad reps + คำนวณ % accuracy
- **Plank:** จับเวลาที่ "ฟอร์มถูก" เท่านั้น (ฟอร์มเพี้ยน = นาฬิกาหยุด)
- ระบบ Sets & Reps: ตั้งเป้าหมาย (เช่น 3 เซ็ต เซ็ตละ 10 ครั้ง), มีนับถอยหลัง, พักระหว่างเซ็ต
- เสียงพูดภาษาไทย: อธิบายท่าก่อนเริ่ม, ชมเมื่อทำถูก, เตือนเมื่อทำผิด
- ระบบสมาชิก: สมัคร/ยืนยัน OTP ทางอีเมล/ล็อกอิน/ลืมรหัส/แก้โปรไฟล์
- บันทึกประวัติ + แดชบอร์ดเปรียบเทียบสถิติตัวเองกับค่าเฉลี่ยผู้ใช้ทั้งระบบ และชี้ "จุดอ่อน" ที่ทำผิดบ่อย
- เล่นแบบ Guest ได้ (ไม่ล็อกอิน) แต่จะชวนให้ล็อกอินเพื่อเซฟผล

---

## 2. ปัญหาและแรงจูงใจ

- การออกกำลังกายผิดฟอร์มเป็นสาเหตุหลักของการบาดเจ็บ (เข่า, หลัง, ไหล่)
- เทรนเนอร์ส่วนตัวมีค่าใช้จ่ายสูง และไม่ได้อยู่ด้วยตลอดเวลา
- คนออกกำลังกายที่บ้าน (home workout) ไม่มีใครคอยดูฟอร์มให้
- **แนวคิด:** ใช้กล้องที่ทุกคนมี + AI มาทำหน้าที่ "เทรนเนอร์" คอยดูฟอร์มและเตือนแบบทันที ฟรี และทำซ้ำได้ตลอด

---

## 3. Tech Stack

| ส่วน | เทคโนโลยี | หน้าที่ |
|------|-----------|---------|
| **Frontend** | React 19 + Vite | UI / SPA |
| | Tailwind CSS 4 | สไตล์ (ธีมดำนีออน) |
| | React Router 7 | จัดการหน้า (routing) |
| | Axios | เรียก REST API |
| | Recharts | กราฟในแดชบอร์ด |
| | SweetAlert2 | กล่อง popup สวย ๆ |
| | Web Speech API | เสียงพูดภาษาไทย (built-in browser) |
| **Backend** | FastAPI + Uvicorn | API server (REST + WebSocket) |
| | WebSocket | สตรีมเฟรมวิดีโอ real-time |
| **AI / ML** | MediaPipe Pose | ตรวจจับ 33 จุดข้อต่อ (landmarks) |
| | scikit-learn | โมเดลจำแนกท่า (RF / GB / MLP) |
| | OpenCV | ถอดรหัสและประมวลผลภาพ |
| | NumPy | คำนวณเวกเตอร์/มุม |
| | joblib | โหลด/บันทึกโมเดล (.pkl) |
| **Database** | PostgreSQL + psycopg2 | เก็บผู้ใช้ + ประวัติการเล่น |
| **Auth** | python-jose (JWT) | โทเคนล็อกอิน (อายุ 7 วัน) |
| | passlib + bcrypt | เข้ารหัสรหัสผ่าน |
| | smtplib (Gmail SMTP) | ส่ง OTP ทางอีเมล |

---

## 4. สถาปัตยกรรมระบบ

ระบบเป็นแบบ **Client–Server** แยกชัดเจน และผมเลือกออกแบบเป็น **"Plan A — ประมวลผล MediaPipe ฝั่ง Server"**

```
┌─────────────────────────┐         ┌──────────────────────────────────┐
│      BROWSER (React)     │         │        FASTAPI BACKEND           │
│                          │         │                                  │
│  📷 Webcam               │         │  ┌────────────────────────────┐  │
│   │ จับเฟรมทุก 100ms     │         │  │  WebSocket Endpoints       │  │
│   │ → JPEG base64        │  WS     │  │  /exercise/squat           │  │
│   └──────────────────────┼────────▶│  │  /exercise/pushup          │  │
│                          │  เฟรม   │  │  /exercise/plank           │  │
│  🎨 Canvas วาด skeleton  │◀────────┼──┤                            │  │
│  🔊 Web Speech พูดไทย    │  ผลลัพธ์│  └──────────┬─────────────────┘  │
│                          │  (JSON) │             │ ต่อ 1 connection   │
│  REST API (Axios):       │         │  ┌──────────▼─────────────────┐  │
│   - /auth/*  (สมาชิก)    │  HTTP   │  │ Predictor (1 instance/คน)  │  │
│   - /exercise/workouts   │◀───────▶│  │  1. OpenCV decode          │  │
│   - /exercise/dashboard  │         │  │  2. MediaPipe → landmarks  │  │
│                          │         │  │  3. Feature Engineering    │  │
└─────────────────────────┘         │  │  4. sklearn predict        │  │
                                     │  │  5. Smoothing + RepCounter │  │
         ┌──────────────┐           │  └────────────────────────────┘  │
         │  PostgreSQL  │◀──────────┤  REST: save/get workouts         │
         │  users       │           └──────────────────────────────────┘
         │  workouts    │
         │  blacklist   │
         └──────────────┘
```

**ทำไมเลือก "Plan A" (ประมวลผลฝั่ง server)?**
- โมเดล AI + MediaPipe + logic ทั้งหมดอยู่ที่เดียว → แก้ไข/อัปเดต/เทรนใหม่ได้โดยไม่ต้องแตะ frontend
- ฝั่ง browser เบา ไม่ต้องโหลดไลบรารี MediaPipe หนัก ๆ → เปิดในเครื่องสเปกต่ำได้
- ความลับของโมเดล (.pkl) ไม่หลุดไปอยู่ฝั่ง client
- **Trade-off ที่ยอมรับ:** มี network latency เพิ่ม → แก้ด้วยการส่งแค่ 10 fps (ทุก 100ms) และบีบภาพเป็น JPEG คุณภาพ 0.7

---

## 5. Flow การทำงาน (ตั้งแต่กล้องถึงผลลัพธ์)

1. **เปิดกล้อง** — `TrainPage` ขอสิทธิ์กล้องด้วย `navigator.mediaDevices.getUserMedia` (640×480)
2. **ตั้งเป้าหมาย** — ผู้เล่นเลือกจำนวนเซ็ต และครั้ง/วินาทีต่อเซ็ต
3. **เริ่ม** — AI พูดอธิบายท่า (TTS) → นับถอยหลัง 5 วินาที → เริ่มจับภาพ
4. **จับเฟรม** — `useExerciseWS` hook วาดวิดีโอลง canvas ชั่วคราว → แปลงเป็น base64 JPEG → ส่งผ่าน WebSocket ทุก 100ms (มี flag `sendingRef` กันส่งซ้อนกัน)
5. **Backend รับเฟรม** — `Predictor.predict()`:
   - `cv2.imdecode` ถอดรหัสภาพ → แปลง BGR→RGB
   - `MediaPipe Pose.process()` ได้ 33 landmarks (x, y, z, visibility)
   - **ด่านที่ 1:** ถ้าไม่เจอคน → ส่ง `pose_detected: false`
   - **ด่านที่ 2 (`is_body_fully_visible`):** เห็นคนแต่ไม่เต็มตัว → ไม่นับ (กันการนับมั่ว)
   - **Feature Engineering:** `landmarks_to_feature_dict()` แปลง landmarks เป็น features หลายสิบตัว
   - `model.predict_proba()` → ความน่าจะเป็นของแต่ละคลาส
6. **Smoothing** — เก็บผลทำนาย 7 เฟรมล่าสุด แล้วใช้เสียงข้างมาก (majority vote) → กันป้ายกระพริบ
7. **นับครั้ง/จับเวลา** — `RepCounter` หรือ `HoldTimer` (state machine)
8. **ส่งกลับ** — JSON: `{ label, confidence, feedback, reps, good_count, bad_count, bad_details, state, proba, landmarks, pose_detected }`
9. **Frontend แสดงผล:**
   - วาด skeleton สีตาม label (เขียว=ดี, เหลือง=เตือน, แดง=ผิดหนัก)
   - แสดง flash message + เสียงพูด (ชม/เตือน) ภาษาไทย
   - อัปเดตตัวเลข reps / timer / accuracy
10. **ครบเป้าหมาย** — พักระหว่างเซ็ต หรือจบ session → รวมสถิติทุกเซ็ต → POST เข้า DB

---

## 6. หัวใจของระบบ AI (มีดีอยู่ตรงนี้)

### 6.1 ไม่ได้ใช้รูปภาพดิบ — ใช้ "พิกัดข้อต่อ" เป็น Feature
MediaPipe แปลงคนในภาพให้เป็นจุด 33 จุด (หัว, ไหล่, ศอก, ข้อมือ, สะโพก, เข่า, ข้อเท้า ฯลฯ) ทำให้:
- ลดมิติข้อมูลมหาศาล (ภาพหลายแสน pixel → เหลือพิกัดไม่กี่สิบตัว)
- ตัดเรื่องสีเสื้อ/พื้นหลัง/แสง → โมเดลโฟกัสที่ "ท่าทาง" จริง ๆ
- ใช้โมเดล ML เบา ๆ (ไม่ต้องใช้ Deep Learning / CNN) ก็แม่นได้

### 6.2 Normalization — ทำให้ทนต่อระยะและตำแหน่งกล้อง
ก่อนเข้าโมเดล เราจัดพิกัดให้เป็นมาตรฐาน:
- **เลื่อนจุดศูนย์กลาง** ไปที่สะโพก (translation invariant — ยืนตรงไหนในเฟรมก็ได้)
- **หารด้วย scale** (ระยะสะโพก–ข้อเท้า สำหรับ squat / สะโพก–ไหล่ สำหรับ plank) → (scale invariant — อยู่ใกล้/ไกลกล้องก็ได้)

### 6.3 Feature Engineering — สกัด "ความรู้เชิงกายภาพ" ออกมา
นอกจากพิกัดดิบ เรายังคำนวณ feature ที่สื่อความหมายของท่าโดยตรง เช่น:
- **มุมข้อต่อ** — มุมเข่า, สะโพก, หลัง, คอ, ศอก (ใช้ dot product ของเวกเตอร์)
- **ความสมมาตรซ้าย-ขวา** (`knee_angle_symmetry` ฯลฯ) — เอียงข้างไหม
- **คะแนนเฉพาะท่า:**
  - Squat: `heel_lift` (ส้นเท้าลอย), `knee_over_toe` (เข่าเลยปลายเท้า), `squat_depth_score` (ย่อลึกพอไหม), `back_lean_score`
  - Push-up: `hip_sag_score` (สะโพกตก/โด่ง เทียบกับเส้นตรงไหล่-ข้อเท้า), `elbow_angle_avg`, `head_drop_score`
  - Plank: `hip_deviation_score`, `body_straightness`, `neck_deviation_score`, `body_alignment_angle`

### 6.4 โมเดล Machine Learning
- เทรน 3 อัลกอริทึมเทียบกัน: **Random Forest, Gradient Boosting, MLP (Neural Network)** ใน `sklearn.Pipeline` (มี `StandardScaler` ด้วย)
- เลือกตัวที่ได้คะแนนสูงสุดจาก **5-fold Stratified Cross-Validation** มาใช้จริง (ในไฟล์โมเดลที่ deploy: squat=MLP, pushup=XGBoost, plank=RandomForest)
- บันทึกเป็น "bundle" `.pkl` ที่รวม `model + label_encoder + feature_columns` ไว้ในไฟล์เดียว → ฝั่ง service โหลดมาใช้ได้ตรง ๆ และมั่นใจว่าลำดับ feature ตรงกัน
- **Dataset:** ประมาณ 40,000 เฟรม สกัดจากวิดีโอคนทำท่าถูก/ผิดแบบต่าง ๆ (เก็บเป็น CSV ของ landmark)

### 6.5 Logic การนับที่ "ฉลาด" (กันการนับมั่ว/กระพริบ)
- **Squat — "Basket Logic":** ตลอด 1 รอบการย่อ-ยืน ระบบจะ "จดความผิด" ที่ตรวจเจอลงตะกร้า พอยืนขึ้นสุด (จบรอบ) ถึงจะสรุป: ถ้าตะกร้ามีความผิดอย่างน้อย 1 อย่าง → นับเป็น bad rep พร้อมจดว่าผิดเรื่องอะไร (ลง `bad_details`) → กันการนับ good/bad สลับไปมากลางรอบ
  - State machine ใช้ `hip/knee ratio` (สะโพกต่ำกว่าเข่า = DOWN)
  - Confidence gating: ถ้าทำนาย "ส้นเท้าลอย" แต่มั่นใจไม่ถึง 70% → ปล่อยผ่าน (กันมุมกล้องเพี้ยน)
- **Push-up — "Patience 4 frames":** ต้องตรวจเจอความผิดชื่อเดิมซ้ำ 4 เฟรมติด ถึงจะนับว่าผิดจริง (กัน noise ชั่วขณะ) + state machine จากมุมศอก (DOWN เมื่อ <120°, UP เมื่อ >145°)
- **Plank — HoldTimer:** เวลาจะเดินเฉพาะตอน `plank_good` เท่านั้น พอฟอร์มเพี้ยนนาฬิกาหยุดทันที + มี **"Lying-flat cheat detector"** เช็คว่าถ้าข้อศอกกับไหล่อยู่ระดับ y ใกล้กันมาก (<5%) = แอบนอนราบโกง → บังคับตีเป็นฟอร์มผิด

---

## 7. โครงสร้างโค้ดแบบไฟล์ต่อไฟล์

```
Machine/
├── backend/
│   ├── server.py              # จุดเริ่ม FastAPI + CORS + รวม router
│   ├── auth.py                # JWT, bcrypt, OTP, ส่งอีเมล, blacklist
│   ├── schemas.py             # Pydantic models (validation)
│   ├── routes/
│   │   ├── auth_route.py       # API สมาชิกทั้งหมด (register/login/otp/profile…)
│   │   └── exercise_route.py   # REST (workouts/dashboard) + WebSocket 3 ท่า
│   ├── services/
│   │   ├── squat_service.py    # MediaPipe + feature + RepCounter (Basket)
│   │   ├── pushup_service.py    # MediaPipe + feature + RepCounter (Patience)
│   │   └── plank_service.py     # MediaPipe + feature + HoldTimer + cheat detector
│   ├── models/                 # ไฟล์โมเดล .pkl ที่เทรนแล้ว
│   └── data_csv/               # ชุดข้อมูล CSV + สคริปต์เทรน (train_*.py)
│
└── frontend/
    └── src/
        ├── App.jsx             # Routing หลัก + จัดการ state ล็อกอิน
        ├── hooks/
        │   ├── useExerciseWS.js # ⭐ หัวใจ: WebSocket + วาด skeleton + เสียงพูด + config 3 ท่า
        │   ├── useAuth.js       # ฟังก์ชันเรียก API auth ทั้งหมด
        │   └── useHistory.js    # ดึง/บันทึกประวัติ
        └── pages/
            ├── SelectPage.jsx       # หน้าเลือกท่า (เมนูหลัก)
            ├── TrainPage.jsx        # ⭐ หน้าฝึกจริง (กล้อง + สถิติ + เซ็ต)
            ├── DashoardPage.jsx     # แดชบอร์ดกราฟสถิติ
            ├── HistoryPage.jsx      # ประวัติย้อนหลัง
            ├── LoginPage / RegisterPage / VerifyOtpPage
            ├── ForgotPasswordPage / ProfilePage / NotFoundPage
```

**ไฟล์ที่ควรเปิดโชว์ตอนพรีเซนต์ (ถ้าต้องโชว์โค้ด):**
- `backend/services/squat_service.py` → `landmarks_to_feature_dict()` (feature engineering) + `RepCounter` (basket logic)
- `frontend/src/hooks/useExerciseWS.js` → `EXERCISE_CONFIG` + ลูปส่งเฟรม + `drawSkeleton`

---

## 8. ระบบสมาชิก (Auth)

- **สมัครสมาชิก:** กรอกข้อมูล → ระบบส่ง **OTP 6 หลัก** ทางอีเมล (หมดอายุ 5 นาที) → ต้องยืนยัน OTP ก่อนถึงจะล็อกอินได้ (`is_verified`)
- **ล็อกอิน:** ใช้ได้ทั้ง username หรือ email → ได้ **JWT token** (อายุ 7 วัน) เก็บใน `localStorage`
- **รหัสผ่าน:** เข้ารหัสด้วย **bcrypt** (ไม่เก็บ plaintext)
- **ลืม username / ลืมรหัสผ่าน:** ขอผ่านอีเมล/OTP, reset ได้
- **เปลี่ยนอีเมล:** ต้องยืนยันรหัสผ่านเดิม + OTP ส่งไปอีเมลใหม่ (`pending_email`)
- **Logout:** เพิ่ม token เข้า **blacklist** ใน DB → ทุก request จะเช็คว่า token ถูก blacklist หรือยัง
- **Guest mode:** เล่นได้โดยไม่ล็อกอิน แต่พอจบจะ popup ชวนล็อกอินเพื่อเซฟ (เก็บผลไว้ใน `sessionStorage` แล้วเซฟอัตโนมัติหลังล็อกอินสำเร็จ)

---

## 9. ฐานข้อมูล (Database)

PostgreSQL — 3 ตารางหลัก:

| ตาราง | คอลัมน์สำคัญ | หน้าที่ |
|-------|--------------|---------|
| `users` | id, username, email, password_hash, is_verified, otp_code, otp_expire_at, pending_email | ข้อมูลสมาชิก |
| `workouts` | user_id, exercise, reps, good, bad, accuracy, bad_details (JSON), total_time, created_at | ประวัติการเล่นแต่ละ session |
| `token_blacklist` | token | โทเคนที่ logout แล้ว |

**Dashboard query เด่น:**
- `GROUP BY exercise` หาสถิติรวมของผู้ใช้ + ค่าเฉลี่ย global → เอามาเทียบ "คุณเหนือ/ต่ำกว่าค่าเฉลี่ย"
- รวม `bad_details` (JSON) ของทุก session → จัดอันดับ "จุดอ่อน" (ทำผิดเรื่องไหนบ่อยสุด)

---

## 10. จุดเด่นทางเทคนิคที่ควรเน้นตอนพูด

1. **Real-time ML ผ่าน WebSocket** — ไม่ใช่อัปโหลดวิดีโอแล้วรอ แต่วิเคราะห์สด ๆ ทีละเฟรม (10 fps) แล้วตอบกลับทันที
2. **Pose-based features แทน raw image** — เลือกใช้ landmark + feature engineering ทำให้โมเดลเบา เทรนเร็ว และอธิบายได้ (explainable) ว่าทำไมตัดสินว่าผิด
3. **Normalization (translation + scale invariant)** — ใช้งานได้จริงในสภาพแวดล้อมหลากหลาย ไม่ผูกกับตำแหน่ง/ระยะกล้อง
4. **Logic กันการนับมั่ว** — Smoothing 7 เฟรม + Basket logic + Patience frames + Confidence gating + Cheat detector → ทำให้ตัวเลขน่าเชื่อถือ
5. **UX ครบ** — เสียงพูดไทย, นับถอยหลัง, ระบบเซ็ต, Guest mode, แดชบอร์ดเทียบค่าเฉลี่ย
6. **แยก Frontend/Backend ชัดเจน + สถาปัตยกรรมที่อธิบายเหตุผลได้ (Plan A)**
7. **Privacy by design** — ไม่เก็บภาพ/วิดีโอเลย ประมวลผลเฟรมแล้วทิ้งทันที เก็บแค่ตัวเลขสถิติ

---

## 11. ข้อจำกัดและแผนพัฒนาต่อ

**ข้อจำกัดปัจจุบัน (พูดตรง ๆ ได้ จะดูเป็นมืออาชีพ):**
- ต้องมุมกล้องที่เหมาะ (ส่วนใหญ่เห็นด้านข้าง) และแสงพอ
- รองรับ 1 คนต่อเฟรม
- ความแม่นยำขึ้นกับคุณภาพ MediaPipe และความหลากหลายของ dataset
- มี network latency เพราะประมวลผลฝั่ง server
- JWT เก็บใน `localStorage` (มีความเสี่ยง XSS)

**แผนพัฒนาต่อ:**
- เพิ่มท่า (lunges, sit-up) — ออกแบบไว้รองรับแล้ว (มีโหมด PVP เป็น "coming soon")
- ย้าย JWT ไป httpOnly cookie เพื่อความปลอดภัยขึ้น
- ทำ Connection pool ของ DB (ตอนนี้เปิด/ปิด connection ทุก request)
- เพิ่ม dataset ให้หลากหลายรูปร่าง/มุมกล้อง → แม่นขึ้น
- โหมดออปชัน: ประมวลผลฝั่ง client (Plan B) สำหรับลด latency
- ทำ rep-counting/feedback ให้ละเอียดขึ้น (เช่น บอกระยะองศาเป้าหมาย)

---

## 12. บทพูดพรีเซนต์ (พร้อมจับเวลา)

> รวมประมาณ 6–7 นาที ปรับลด/เพิ่มได้ตามเวลาที่ให้

### 🎤 [0:00–0:45] เปิด — ปัญหา
> "สวัสดีครับ/ค่ะ อาจารย์และเพื่อน ๆ ทุกคน วันนี้กลุ่มของเราขอนำเสนอโปรเจกต์ **AI Form Trainer** ครับ
>
> ทุกคนน่าจะเคยออกกำลังกายที่บ้านใช่ไหมครับ ปัญหาคือเวลาเราเล่นเองไม่มีใครคอยดูว่าเรา 'ทำท่าถูกหรือเปล่า' ซึ่งการทำผิดฟอร์ม เช่น สควอทแล้วหลังงอ หรือวิดพื้นแล้วสะโพกตก เป็นสาเหตุหลักของการบาดเจ็บ และจ้างเทรนเนอร์ส่วนตัวก็มีค่าใช้จ่ายสูง
>
> โปรเจกต์ของเราเลยตั้งคำถามว่า — เราใช้แค่ 'กล้องคอม' กับ 'AI' มาทำหน้าที่เป็นเทรนเนอร์ส่วนตัวที่คอยดูฟอร์มและเตือนเราแบบ real-time ได้ไหม"

### 🎤 [0:45–1:30] โปรเจกต์ทำอะไรได้ (Demo overview)
> "ระบบของเรารองรับ 3 ท่าครับ: สควอท, วิดพื้น แบบนับจำนวนครั้ง และแพลงก์ แบบจับเวลา
>
> เมื่อเปิดกล้อง AI จะวาดโครงกระดูกทับตัวเรา แล้ววิเคราะห์ทันทีว่าฟอร์มถูกหรือผิดตรงไหน เช่น 'ส้นเท้าลอย' 'หลังงอ' พร้อม **พูดเตือนเป็นภาษาไทย** ทันที นับจำนวนครั้งให้ แยกว่าครั้งไหนทำถูก ครั้งไหนทำผิด และสรุปเป็นเปอร์เซ็นต์ความแม่นยำ
>
> นอกจากนี้ยังมีระบบสมาชิก เก็บประวัติ และแดชบอร์ดที่บอกได้ว่า 'จุดอ่อน' ที่เราทำผิดบ่อยที่สุดคืออะไร"

### 🎤 [1:30–3:00] สถาปัตยกรรม + Flow (โชว์ภาพ diagram)
> "มาดูเบื้องหลังกันครับ ระบบแบ่งเป็น Frontend ที่เขียนด้วย React และ Backend ที่เป็น FastAPI
>
> หลักการทำงานคือ — ฝั่งเบราว์เซอร์จะจับภาพจากกล้องทุก ๆ 100 มิลลิวินาที หรือ 10 เฟรมต่อวินาที แล้วส่งภาพนั้นผ่าน **WebSocket** ไปที่ Server
>
> ฝั่ง Server จะใช้ **MediaPipe** ของ Google แปลงคนในภาพให้เป็นจุดข้อต่อ 33 จุด จากนั้นเราคำนวณ 'ฟีเจอร์' เช่น มุมเข่า มุมหลัง ความสมมาตรซ้ายขวา แล้วป้อนเข้าโมเดล Machine Learning เพื่อจำแนกว่าเป็นท่าถูกหรือผิดแบบไหน สุดท้ายส่งผลกลับมาให้เบราว์เซอร์วาดโครงกระดูกและพูดเตือน ทั้งหมดนี้เกิดขึ้นภายในเสี้ยววินาที
>
> เราเลือกประมวลผล AI ฝั่ง Server ที่เราเรียกว่า 'Plan A' เพราะทำให้โมเดลอยู่ที่เดียว อัปเดตง่าย และเครื่องผู้ใช้ไม่ต้องแบกภาระโหลดไลบรารีหนัก ๆ"

### 🎤 [3:00–4:30] หัวใจ AI (ส่วนที่ควรเน้นมากสุด)
> "จุดที่เราภูมิใจที่สุดคือฝั่ง AI ครับ
>
> อย่างแรก เราไม่ได้ป้อน 'รูปภาพดิบ' เข้าโมเดล แต่ใช้ 'พิกัดข้อต่อ' แทน ข้อดีคือมันตัดเรื่องสีเสื้อ พื้นหลัง แสง ออกไปหมด โมเดลโฟกัสที่ท่าทางจริง ๆ ทำให้ใช้โมเดลเบา ๆ อย่าง Random Forest หรือ Neural Network เล็ก ๆ ก็แม่นได้ ไม่ต้องใช้ Deep Learning ตัวใหญ่
>
> อย่างที่สอง เราทำ **Normalization** คือย้ายจุดศูนย์กลางไปที่สะโพกและหารด้วยขนาดตัว ทำให้ผู้เล่นจะยืนใกล้หรือไกลกล้อง อยู่ซ้ายหรือขวาของจอ ก็วิเคราะห์ได้เหมือนกัน
>
> อย่างที่สาม คือ logic การนับที่เราออกแบบมาเพื่อ 'กันการนับมั่ว' โดยเฉพาะ เช่น เราใช้เสียงข้างมากจาก 7 เฟรมล่าสุดกันป้ายกระพริบ, ใช้ระบบ 'ตะกร้าความผิด' ที่จะสรุปผลแค่ตอนจบรอบ และในท่าแพลงก์ เรายังมีตัวจับโกง ถ้าใครแอบนอนราบกับพื้นระบบจะรู้ทันทีและหยุดนับเวลาให้"

### 🎤 [4:30–5:30] เดโม่สด (ถ้าทำได้)
> "เดี๋ยวผมขอสาธิตให้ดูครับ — ผมจะเลือกท่าสควอท ตั้งเป้า 1 เซ็ต 3 ครั้ง...
> *(ทำท่าถูก 1 ครั้ง → โชว์ว่าขึ้นเขียว + พูดชม)*
> *(แกล้งทำหลังงอ → โชว์ว่าขึ้นแดง + พูดเตือน + นับเป็น bad)*
> จะเห็นว่าระบบแยก good กับ bad ได้ และสรุปเปอร์เซ็นต์ความแม่นยำให้ตอนจบครับ"

### 🎤 [5:30–6:30] สรุป + ข้อจำกัด + อนาคต
> "สรุปนะครับ โปรเจกต์นี้รวมเทคโนโลยีหลายด้านเข้าด้วยกัน ทั้ง Computer Vision, Machine Learning, การสื่อสาร real-time ด้วย WebSocket และ Full-stack web development
>
> ข้อจำกัดที่เรารู้คือ ต้องการมุมกล้องและแสงที่เหมาะสม และยังประมวลผล 1 คนต่อครั้ง ส่วนแผนต่อไปเราอยากเพิ่มท่าให้มากขึ้น และเพิ่มชุดข้อมูลให้หลากหลายเพื่อความแม่นยำที่สูงขึ้น
>
> ขอบคุณครับ/ค่ะ ยินดีรับคำถามครับ"

---

## 13. คำถาม–คำตอบที่อาจโดนถาม

### กลุ่ม A: เกี่ยวกับ AI / ML

**Q1: ใช้โมเดล Machine Learning อะไร ทำไมไม่ใช้ Deep Learning / CNN?**
> ใช้โมเดลใน scikit-learn ครับ — เทรนเทียบ 3 แบบคือ Random Forest, Gradient Boosting และ MLP (Neural Network เล็ก ๆ) แล้วเลือกตัวที่ cross-validation ดีที่สุดมาใช้แต่ละท่า เหตุผลที่ไม่ใช้ CNN เพราะ **MediaPipe ทำงานหนักเรื่องดึงภาพให้เราแล้ว** เราได้ออกมาเป็นพิกัดข้อต่อ ซึ่งเป็นข้อมูลเชิงตัวเลขที่มีมิติต่ำ การใช้โมเดล ML ทั่วไปจึงเพียงพอ เทรนเร็ว ใช้ข้อมูลน้อยกว่า และที่สำคัญคือ **อธิบายได้** ว่าทำไมตัดสินว่าผิด ขณะที่ CNN จะกินทรัพยากรและข้อมูลมหาศาลกว่ามากโดยไม่จำเป็น

**Q2: ความแม่นยำ (accuracy) เท่าไหร่? วัดยังไง?**
> วัดด้วย **5-fold Stratified Cross-Validation** บนชุดข้อมูลที่ split train/test 80/20 ครับ การใช้ stratified ทำให้แต่ละ fold มีสัดส่วนคลาส good/bad เท่ากัน ไม่ลำเอียง และเราเลือกโมเดลจากค่า CV เฉลี่ยที่สูงสุด ไม่ได้ดูแค่ test accuracy ครั้งเดียวเพื่อกัน overfitting
> *(ถ้ามีตัวเลขจริงให้ใส่ตรงนี้ เช่น "ได้ประมาณ 9X%" — รันสคริปต์ train ดูค่าก่อนพรีเซนต์)*

**Q3: เก็บข้อมูล (dataset) มายังไง? มีกี่ตัวอย่าง?**
> เราอัดวิดีโอคนทำท่าทั้งแบบถูกและแบบผิดในรูปแบบต่าง ๆ (เช่น สควอทส้นเท้าลอย, หลังงอ) แล้วรันผ่าน MediaPipe เพื่อสกัดพิกัดข้อต่อของทุกเฟรมออกมาเก็บเป็นไฟล์ CSV แยกตามประเภท รวมทั้งหมดประมาณ **40,000 เฟรม** ครับ

**Q4: ทำไมต้อง normalize feature?**
> เพื่อให้โมเดล **ทนต่อตำแหน่งและระยะของกล้อง** ครับ ถ้าใช้พิกัดดิบ พอผู้เล่นขยับเข้าใกล้กล้องตัวเลขจะใหญ่ขึ้นหมด หรือยืนคนละมุมจอค่าก็เปลี่ยน เราจึงเลื่อนจุดอ้างอิงไปที่สะโพก (translation invariant) และหารด้วยขนาดลำตัว (scale invariant) ทำให้ "ท่าเดียวกัน" ให้ค่าใกล้เคียงกันเสมอ ไม่ว่าจะถ่ายใกล้หรือไกล

**Q5: feature ที่ใช้มีอะไรบ้าง?**
> มีหลายสิบตัวครับ หลัก ๆ คือ (1) พิกัด x,y,z ของข้อต่อที่ normalize แล้ว (2) มุมข้อต่อ เช่น มุมเข่า มุมสะโพก มุมหลัง มุมศอก มุมคอ (3) ความสมมาตรซ้าย-ขวา และ (4) คะแนนเฉพาะท่า เช่น คะแนนส้นเท้าลอย คะแนนสะโพกตก คะแนนความตรงของลำตัว

**Q6: จัดการกับการทำนายที่ "กระพริบ" (เดี๋ยวถูกเดี๋ยวผิด) ยังไง?**
> ใช้หลายชั้นครับ — (1) **Smoothing** เก็บผล 7 เฟรมล่าสุดแล้วโหวตเสียงข้างมาก (2) **Patience frames** ในท่าวิดพื้น ต้องเจอความผิดเดิมซ้ำ 4 เฟรมติดถึงนับ (3) **Confidence gating** ถ้ามั่นใจไม่ถึงเกณฑ์ก็ไม่ฟันธงว่าผิด

### กลุ่ม B: เกี่ยวกับการนับ / Logic

**Q7: นับจำนวนครั้ง (reps) ยังไงไม่ให้นับซ้ำ?**
> ใช้ **state machine** ครับ ท่าสควอทดูจากอัตราส่วนความสูงสะโพกกับเข่า ถ้าสะโพกต่ำกว่าเข่า = สถานะ DOWN พอยืนกลับขึ้น = UP แล้วบวกหนึ่งครั้ง การนับเกิดแค่ตอนเปลี่ยนจาก DOWN→UP ครั้งเดียว ไม่ใช่ทุกเฟรม จึงไม่นับซ้ำ ส่วนวิดพื้นใช้มุมศอกแทน (งอ<120° = DOWN, เหยียด>145° = UP)

**Q8: แล้วระบบ "Basket Logic" คืออะไร?**
> เป็น logic ที่เราออกแบบเองครับ — ระหว่าง 1 รอบการเคลื่อนไหว เราจะ "จด" ความผิดทุกอย่างที่ตรวจเจอลงในตะกร้า พอจบรอบ (ยืนขึ้นสุด) ถึงค่อยสรุปทีเดียวว่า ถ้าตะกร้ามีความผิดอย่างน้อย 1 อย่าง รอบนั้นนับเป็น bad และจดด้วยว่าผิดเรื่องอะไรบ้าง ข้อดีคือมันไม่สับสนเวลากลางรอบมีทั้งเฟรมถูกและผิดปนกัน

**Q9: ท่าแพลงก์จับเวลายังไง? ป้องกันคนโกงยังไง?**
> นาฬิกาจะเดินเฉพาะตอนที่โมเดลบอกว่าฟอร์มถูก (`plank_good`) เท่านั้นครับ พอฟอร์มเพี้ยน เช่น สะโพกตก เวลาจะหยุดทันที และเรามี **ตัวจับโกง** เพิ่ม คือถ้าตรวจเจอว่าข้อศอกกับหัวไหล่อยู่ในระดับความสูงใกล้กันมากเกินไป แปลว่าคนคนนั้นแอบนอนราบกับพื้น ระบบจะบังคับตีเป็นฟอร์มผิดและไม่นับเวลาให้

**Q10: accuracy เปอร์เซ็นต์ที่โชว์ผู้ใช้คำนวณยังไง?**
> `accuracy = good_reps / (good_reps + bad_reps) × 100` ครับ คือสัดส่วนครั้งที่ทำฟอร์มถูกต่อจำนวนครั้งทั้งหมด รวมทุกเซ็ตใน session นั้น

### กลุ่ม C: เกี่ยวกับสถาปัตยกรรม / ระบบ

**Q11: ทำไมเลือกประมวลผลฝั่ง Server ไม่ทำบน Browser?**
> เราเรียกแนวทางนี้ว่า Plan A ครับ ข้อดีคือ (1) โมเดลและ logic อยู่ที่เดียว แก้/เทรนใหม่ได้โดยไม่ต้องอัปเดตฝั่ง client (2) เครื่องผู้ใช้ไม่ต้องแบกโหลดไลบรารี MediaPipe หนัก ๆ เปิดในเครื่องสเปกต่ำได้ (3) โมเดลไม่หลุดไปฝั่งผู้ใช้ ข้อแลกเปลี่ยนคือมี network latency เพิ่ม เราเลยส่งแค่ 10 เฟรมต่อวินาทีและบีบภาพ JPEG ให้เล็กลง ซึ่งเพียงพอสำหรับการให้ feedback ที่ผู้ใช้รู้สึกว่า real-time

**Q12: ทำไมใช้ WebSocket ไม่ใช้ REST API ธรรมดา?**
> เพราะเป็นการสตรีมข้อมูลต่อเนื่องแบบสองทางครับ ถ้าใช้ REST จะต้องเปิด-ปิด connection ใหม่ทุกเฟรม ซึ่งมี overhead สูงมากและช้า WebSocket เปิด connection ค้างไว้เส้นเดียว ส่งเฟรมเข้า-รับผลออกได้ตลอด เหมาะกับงาน real-time มากกว่า

**Q13: ถ้าหลายคนเล่นพร้อมกัน ระบบรับไหวไหม?**
> แต่ละ WebSocket connection จะสร้าง Predictor และ MediaPipe instance ของตัวเองแยกกันครับ ทำให้ข้อมูลผู้เล่นไม่ปนกัน แต่ข้อจำกัดคือ MediaPipe + ML กิน CPU พอสมควร ถ้าคนเยอะมากต้อง scale เครื่อง server หรือทำ worker เพิ่ม ซึ่งเป็นสิ่งที่เราวางแผนพัฒนาต่อ

**Q14: ถ้า WebSocket หลุดกลางคันจะเกิดอะไรขึ้น?**
> ฝั่ง frontend มีการเช็คสถานะ connection (`connecting/connected/disconnected`) และพยายามเชื่อมต่อใหม่เมื่อหน้ายัง active อยู่ครับ ส่วนฝั่ง backend ถ้า client หลุด จะ catch `WebSocketDisconnect` แล้วปิด MediaPipe instance ทิ้งเพื่อคืน memory

**Q15: ส่งภาพ 10 fps พอไหม ทำไมไม่ส่ง 30 fps?**
> เป็นการ trade-off ระหว่างความลื่นกับภาระ server ครับ 10 fps เพียงพอให้ feedback ที่คนรู้สึกว่าทันที และการนับครั้งก็ยังแม่น เพราะท่าออกกำลังกายไม่ได้เร็วระดับต้องการ 30 fps ส่วน 30 fps จะเพิ่มภาระ CPU และ bandwidth สามเท่าโดยไม่ได้ประโยชน์เพิ่มชัดเจน

### กลุ่ม D: เกี่ยวกับความปลอดภัย / Privacy

**Q16: เก็บวิดีโอ/ภาพของผู้ใช้ไหม? เรื่อง privacy เป็นยังไง?**
> **ไม่เก็บเลยครับ** ออกแบบเป็น privacy by design เฟรมที่ส่งมาประมวลผลเสร็จแล้วทิ้งทันที ไม่มีการบันทึกลงดิสก์ สิ่งที่เก็บลงฐานข้อมูลมีแค่ตัวเลขสถิติ เช่น จำนวนครั้ง เปอร์เซ็นต์ จุดอ่อน ไม่มีภาพใบหน้าหรือวิดีโอใด ๆ

**Q17: ระบบรหัสผ่านปลอดภัยไหม?**
> รหัสผ่านเข้ารหัสด้วย **bcrypt** ก่อนเก็บ ไม่เก็บ plaintext ครับ การล็อกอินใช้ **JWT** ที่เซ็นด้วย secret key ฝั่ง server และมีระบบ **OTP ทางอีเมล** ยืนยันตัวตนตอนสมัครและตอนรีเซ็ตรหัส ส่วนตอน logout เราเพิ่ม token เข้า blacklist เพื่อให้ token เดิมใช้ไม่ได้อีก

**Q18: JWT เก็บที่ localStorage ไม่เสี่ยง XSS เหรอ?**
> ถูกต้องครับ นี่เป็นข้อจำกัดที่เรารู้ — `localStorage` เข้าถึงได้ด้วย JavaScript จึงเสี่ยงต่อ XSS แผนพัฒนาต่อคือย้ายไปเก็บใน **httpOnly cookie** ที่ JavaScript อ่านไม่ได้ ซึ่งปลอดภัยกว่า

### กลุ่ม E: คำถามกว้าง / เชิงแนวคิด

**Q19: จุดเด่นที่ต่างจากแอปออกกำลังกายทั่วไปคืออะไร?**
> แอปทั่วไปส่วนใหญ่แค่ "นับครั้ง" หรือ "เล่นวิดีโอสอน" แต่ของเรา **วิเคราะห์ฟอร์มและบอกว่าผิดตรงไหนแบบเรียลไทม์** เหมือนมีเทรนเนอร์ยืนข้าง ๆ คอยเตือน บวกกับแดชบอร์ดที่ชี้ "จุดอ่อนเฉพาะตัว" ของผู้ใช้แต่ละคน

**Q20: เจอปัญหาอะไรหนักสุดตอนทำ แล้วแก้ยังไง?**
> ปัญหาใหญ่คือผลทำนายมัน "กระพริบ" และนับครั้งมั่ว ตอนแรกตัวเลขเด้งไปมาไม่น่าเชื่อถือเลยครับ เราแก้ด้วยการซ้อนหลายเทคนิค ทั้ง smoothing เสียงข้างมาก, basket logic ที่สรุปแค่ตอนจบรอบ, patience frames และ confidence gating กว่าจะจูนค่า threshold ให้ลงตัวก็ใช้เวลาทดสอบกับท่าจริงเยอะพอสมควร

**Q21: แบ่งงานในทีมยังไง?** *(เตรียมตอบตามจริงของกลุ่ม)*
> เช่น "คนหนึ่งดูแลฝั่ง AI/ML กับการเก็บ dataset, คนหนึ่งทำ backend FastAPI กับฐานข้อมูล, อีกคนทำ frontend React และ UX" — ปรับตามจริงครับ

**Q22: ถ้ามีเวลาต่ออีก 1 เดือน จะทำอะไรเพิ่ม?**
> เพิ่มจำนวนท่า, เพิ่ม dataset ให้หลากหลายรูปร่าง/มุมกล้องเพื่อความแม่นยำ, ทำ DB connection pooling เพื่อรองรับผู้ใช้พร้อมกันมากขึ้น, และทำโหมดประมวลผลฝั่ง client เป็นทางเลือกเพื่อลด latency ครับ

---

## ✅ เช็กลิสต์ก่อนขึ้นพรีเซนต์
- [ ] รันสคริปต์ `train_*.py` ดูค่า **accuracy/CV จริง** มาเติมใน Q2 (กรรมการชอบถามตัวเลข)
- [ ] เตรียม **เดโม่สด** — เปิด backend (`uvicorn server:app`) + frontend (`npm run dev`) ทดสอบให้ผ่านก่อน
- [ ] เผื่อเดโม่พัง — อัดคลิปวิดีโอ backup ไว้
- [ ] เตรียมไฟล์โชว์โค้ด 2 ไฟล์: `squat_service.py` (feature + basket) และ `useExerciseWS.js`
- [ ] ซ้อมพูดให้อยู่ในเวลา และแบ่งกันพูดให้ชัดว่าใครพูดช่วงไหน
- [ ] เช็กว่ากล้อง + ไมค์ + เสียง TTS ทำงานในห้องพรีเซนต์
